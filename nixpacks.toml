[phases.setup]
# On ajoute 'python311Packages.pillow' pour corriger l'erreur "No module named PIL"
nixPkgs = [
    "python311",
    "python311Packages.pip",
    "python311Packages.discordpy",
    "python311Packages.openai",
    "python311Packages.python-dotenv",
    "python311Packages.xhtml2pdf",
    "python311Packages.pillow"
]

[phases.install]
cmds = ["echo 'Installation via Nix terminée'"]

[start]
cmd = "python3 bot.py"

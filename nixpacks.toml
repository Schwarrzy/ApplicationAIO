[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "cairo", "pkg-config", "gobject-introspection", "gcc"]

[phases.install]
# On installe directement dans le système Nix sans 'venv'
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python3 bot.py"
